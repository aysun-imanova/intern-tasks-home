import logic.CalculateDigits;

/**
 * 1) Verilmiş ədədin tərkibindəki ən böyük rəqəmi tapan alqoritm (Misal input 1341, output 4)
 * Qeyd: tapşırıqda müqayisəni if-lə deyil, Math.max(digit, largestDigit) methodu ilə də etmək mümkündür.
 */
public class Main {
    public static void main(String[] args) {
        CalculateDigits.calculateLargestDigit();
    }
}
